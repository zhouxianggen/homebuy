<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hg.www.selller">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".EditCommodityActivity"
            android:label="@string/edit_commodity"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Demo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CommodityGroupListActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".ui.PrepareOrderListActivity" />
        <activity android:name=".ui.NewOrderListActivity" />
        <activity android:name=".ui.AcceptedOrderListActivity" />
        <activity android:name=".ui.LoadedOrderListActivity" />
        <activity android:name=".ui.PaidOrderListActivity" />
        <activity android:name=".ui.ReturnedOrderListActivity" />
        <activity android:name=".ui.ClaimedOrderListActivity"></activity>
    </application>

</manifest>
